rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Public read for live site data; writes require Firebase Auth (admin account).
    match /published/site {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
